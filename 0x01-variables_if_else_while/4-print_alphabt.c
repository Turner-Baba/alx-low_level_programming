#include<stdio>

/**
 * main - Entry point of main function
 * Return: always 0
 */

int main(void)
{
	char n;

	n = 'a';
	while
		(n <= 'z')
	{
			if ((n != 'q' && n != 'e') && n <= 'z' n++)
				putchar(n);
	}
	putchar('\n');
	return (0);
}
